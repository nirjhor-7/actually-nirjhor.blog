window.addEventListener("themechange", (e) => { const isDark = e.detail.theme === "dark"; const container = document.getElementById("starry-background"); if (isDark && container) { container.style.opacity = "1"; } else if (container) { container.style.opacity = "0"; container.style.pointerEvents = "none"; } }); window.addEventListener("DOMContentLoaded", () => { const isDark = document.documentElement.getAttribute("saved-theme") === "dark"; const container = document.getElementById("starry-background"); if (container) { if (isDark) { container.style.opacity = "1"; container.style.pointerEvents = "none"; } else { container.style.opacity = "0"; container.style.pointerEvents = "none"; } } });